[workspace]
members = ["mirage-core", "mirage-watm", "mirage-server", "mirage-provisioner"]
resolver = "2"

# Release profile for WASM builds (mirage-watm)
[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
